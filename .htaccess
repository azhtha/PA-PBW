<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Kirim semua permintaan ke folder public, kecuali request yang sudah langsung mengarah ke /public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]

</IfModule>
